name: Release

on:
  push:
    branches:
      - release

permissions:
  contents: write

jobs:
  release:
    name: Release
    uses: ./.github/workflows/_release.yml
    with:
      release-name: ${{ github.event.repository.name }}
    secrets: inherit
